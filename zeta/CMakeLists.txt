# ── Per-module CMake (headers-only modules, listed for IDE discovery) ─

# Each subdirectory has its own CMakeLists.txt that just registers
# headers so they show up in IDE project trees.
add_subdirectory(bits)
add_subdirectory(memory)
add_subdirectory(random)
add_subdirectory(status)
add_subdirectory(strings)
add_subdirectory(container)
