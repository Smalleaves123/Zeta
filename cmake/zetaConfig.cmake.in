set(ZETA_INCLUDE_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../include")

include("${CMAKE_CURRENT_LIST_DIR}/zetaTargets.cmake")

if(NOT TARGET zeta)
    add_library(zeta INTERFACE IMPORTED)
    set_target_properties(zeta PROPERTIES
        INTERFACE_LINK_LIBRARIES zeta::zeta)
endif()
